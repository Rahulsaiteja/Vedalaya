PORT=5001

# Node.js backend URL — set to your deployed backend URL in production
# e.g. https://your-backend.onrender.com
NODE_API_URL=http://localhost:5000

# Shared secret for webhook to Node backend (must match WEBHOOK_SECRET in backend)
WEBHOOK_SECRET=change_me_to_a_random_secret
